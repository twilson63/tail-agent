var _ = require('underscore');

module.exports = function (opts, required) {
  var req = _(required).filter(function (el) {
    var valid = _.has(opts, el);
    if (!valid) console.log('%s is required', el);
    return valid;
  });

  // return true if required opts are present
  return req.length > 0;
};
