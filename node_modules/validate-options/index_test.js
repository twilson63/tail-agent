var test = require('tap').test;
var validate = require('./');

test('', function(t) {
  t.ok(!validate({}, ['log', 'port']));
  t.ok(validate(
    {log: 'foo', port: 4000},
    ['log', 'port']
  ));
  t.end();
});
